import { ConnectedPosition } from '@angular/cdk/overlay';

export interface HtmlTooltipCdkManagedPositionOptions {
  positions: ConnectedPosition[];
}

export interface HtmlTooltipOffsetFromOriginPositionOptions {
  offsetX: number;
  offsetY: number;
}
